# 阶段 3 独立审查记录

- 审查身份：Codex 独立只读审查代理（`/root/phase0_independent_review`）。
- 最终输入提交：`0666583`（包含阶段 3 实现 `6671216`、解析故障修复 `665e5a8` 和 ID 规范化修复）。
- 审查时间：2026-08-09（Asia/Tokyo）。
- 审查结论：**通过**；P0/P1/P2 均无。
- 工作区：`git diff --check` 通过，未修改实现工作区。

## 修复核验

- Neo4j 实体解析异常会生成不含猜测 node ID 的 `GraphFact(status=unavailable)`，限制为 `GRAPH_UNAVAILABLE`，不调用旧 Router，并写入目标图审计事件。
- `max_candidates` 严格接受 JSON 整数；布尔值、字符串和浮点值均拒绝。
- Recipe、步骤、Ingredient、TechniqueDoc、菜系候选 ID 均在验证后写回去除首尾空白的稳定值。
- 五个固定模板仍拒绝自由 Cypher、未知 label/关系、未知参数和越界 `limit`。

## 自动化验证

- 阶段 3 与阶段 2 相关回归：`61 passed`。
- 既有旧链路回归：`16 passed`，1 条既有 pytest 收集警告。
- 全量：`117 passed`，6 条既有 pytest 收集警告；警告均为已有测试替身的 pytest 收集提示。
- 未修改 `rag_modules/hybrid_retrieval.py`、`rag_modules/graph_rag_retrieval.py`、`rag_modules/milvus_index_construction.py`。

## 真实 Neo4j 与 PDS 证据

目标别名：`what-to-eat-neo4j-a2`，database=`neo4j`；查询只读，未执行导入或数据写入。

五个模板均执行 `EXPLAIN`、`PROFILE` 和带 `LIMIT` 的实际查询。PROFILE 树均从稳定 ID 的 `NodeUniqueIndexSeek` 开始：

| template_id | 关系/过滤 | 实际状态 | node ID 结果 |
| --- | --- | --- | --- |
| `recipe_step_anchor_v1` | `Recipe - CONTAINS_STEP -> CookingStep` | `verified` | `201002454`, `201002477` |
| `ingredient_recipes_v1` | `Ingredient <- REQUIRES - Recipe` | `verified` | `201000002`, `201000001` |
| `ingredient_vegetable_pairs_v1` | `Ingredient <- REQUIRES - Recipe - REQUIRES -> Ingredient` | `verified` | `201000002`, `201000001`, `201000004`, `201000008` |
| `technique_chunks_v1` | `TechniqueDoc - HAS_CHUNK -> TechniqueChunk` | `verified` | `tipdoc_b43f2b437984` 及两个 chunk |
| `recipe_cuisine_filter_v1` | `Recipe.cuisineType` 属性过滤 | `verified` | `201002454` |

不存在的 Ingredient ID 实际返回 `GraphFact.status=not_found`、空 node ID；图服务异常替身返回 `unavailable`，两种状态均不携带正文证据。

真实主入口四场景未调用旧 Router：鸡肉到菜谱、宫保鸡丁第一步、腌肉技巧章节为 `verified`；当前实例的鸡肉搭配蔬菜关系为 `not_found`，明确不能用正文证明。步骤和技巧正文均从 PDS build `pds_f01044e524ef43b413f76b02` 回补。

## 开关与回退

- `RETRIEVAL_QUERY_PLAN_ENABLED` 默认 `false`。
- `RETRIEVAL_TARGETED_GRAPH_ENABLED` 默认 `false`。
- 两个开关关闭时回到既有 Router；QueryPlan 开启而目标图关闭时返回显式 `unavailable`，不静默回退或伪造关系。
- 未执行任何 Milvus collection 创建、删除、重建或切换；旧 `cooking_knowledge` 保持不变。

阶段 3 DoD 已通过，可以进入阶段 4；main 合并仍需用户明确确认。
