# 阶段 6a Recipe CSV 生产器签收

- 审查身份：独立只读复核代理 `/root/phase6a_recheck`。
- 审查输入提交：`91d423367028af8b47b938b83cf4773964e4c02f`，相对 `ec5790c`。
- 审查时间：`2026-08-10T00:17:02+08:00`。
- 结论：**阶段 6a passed**。

## 审查结果

未发现 P0、P1 或 P2。前四轮复核发现的工具/器具漏网项均已修复：筷子、手套、冰箱、打火机、搅拌机、粉碎机、纱布、吸管、过滤袋、布及其描述不再生成 `Ingredient` 或 `REQUIRES`。

计算段仅接受规范化后的直接食材名和数量。算术公式、变量、用量说明与自然语言计算前缀均不会生成 `Ingredient`；直接赋值（例如 `八角 = 5g`）仍可解析。输入 manifest 在单次读取的同一字节上完成 JSON 解析和 SHA-256 计算，避免来源选择与溯源摘要不一致。

## 验证证据

- `python -m pytest -q test/test_recipe_graph_producer.py`：`12 passed`。
- `python -m pytest -q`：`177 passed, 6 warnings`；6 条均为既有 pytest 测试替身收集警告。
- 对 `data/dishes` 的 323 份 Markdown 执行两次隔离构建：`nodes.csv`、`relationships.csv`、`recipe-build-manifest.json` 均字节一致。
- 两次工件均通过 `python scripts/validate_recipe_graph_csv.py --input <artifact> --strict`：`4368` 节点、`5953` 关系；外键、`Recipe -> REQUIRES -> Ingredient`、`Recipe -> CONTAINS_STEP -> CookingStep` 方向与步骤顺序均有效。
- manifest 竞态回归确认：载入 `a.md` 后改写输入 manifest，记录的 SHA-256 仍对应初始读取字节，不对应改写后的内容。
- 扩展工具/公式词面审计仅剩食品或包装食品名称，无工具、公式或计算变量污染。

## 边界

本签收仅覆盖阶段 6a 的离线 Recipe Markdown 到 CSV 生产、manifest 和校验器。未启动 Compose，未连接、导入或写入 Neo4j/Milvus，未修改旧 Hybrid/Graph RAG 模块。阶段 6b 的离线评测尚未开始；隔离 staging 导入、备份恢复、rollout 监控与最终场景验收仍受后续门槛约束。
