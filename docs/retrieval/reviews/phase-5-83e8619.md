# 阶段 5 软偏好策略签收

- 签收提交：`83e8619ae68e86f948eebf45a4d3bbf96855f27b`。
- 签收范围：个人本机项目；不构成共享、staging 或生产环境的发布批准。
- 签收人：Codex 执行代理，依据项目所有者 2026-08-09 的自主推进与个人项目门控授权。
- 当前状态：**阶段 5 passed（personal-local）**。

## 审查闭环

阶段 5 的独立只读审查先后检查了 `a488356` 与 `88b92c2`。发现的所有 P1/P2 均已在签收提交及其祖先中修复，并有回归覆盖：

- 严格、医疗、脂肪克数和热量阈值请求现在在目标图、V2、实体直达和旧 Router 前短路为 `evidence_insufficient`，不会展示候选或调用图/向量检索。
- 数值在营养词前后的表达、`g`/克、千卡/大卡/热量及高血压、糖尿病等医疗表达均 fail-closed。
- “低脂川菜”“少油川菜”“清爽川菜”均使用 `recipe_cuisine_filter_v1` 验证的 parent 范围进行 V2 child-chunk 检索、parent 聚合和 PDS hydration，返回 `soft_preference` 证据等级与不能验证严格低脂的限制。
- `RETRIEVAL_STRICT_NUTRITION_ENABLED` 在默认环境、环境变量、直接构造 `GraphRAGConfig` 和 `from_dict` 路径均受当前治理策略收敛为 `false`。

## 验证证据

- 阶段 5 与阶段 3/4 聚焦套件：`78 passed`。
- 全量：`165 passed, 6 warnings`；6 条均为既有 pytest 测试替身收集警告。
- 本机只读 Neo4j/PDS 核验：`recipe_cuisine_filter_v1` 返回 `32` 个川菜 parent ID，与 PDS 范围一致；未进行图、Milvus 或集合写入。
- `scripts/validate_nutrition_dataset.py` 在当前 `nutrition_soft_preference_v1` 下明确拒绝严格模式，防止把文本或相似度当成可信营养数据。

## 残余边界与回退

- 当前没有受治理的营养数值、标签或可复算链路，因此不提供严格低脂、脂肪克数、低热量或医疗饮食结论。
- 回退为保持 `RETRIEVAL_STRICT_NUTRITION_ENABLED=false`；`RETRIEVAL_MILVUS_V2_ENABLED` 默认也保持 `false`。旧 `cooking_knowledge` 和旧检索模块均未修改或删除。
