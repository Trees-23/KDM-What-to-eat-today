# 阶段 6B 独立只读复核

- 审查身份：`phase6b_fixture_rereview`（独立审查代理）
- 输入提交：`94e0898`（修复（检索评测）：以图驱动结果生成关系证据）
- 审查时间：2026-08-10
- profile：`personal-local`
- 结论：**通过，可签收阶段 6B personal-local**

## 复核范围

- 50 条固定释义的 old/new JSONL、报告、fixture、案例、阈值和 manifest SHA-256。
- `HybridRetrievalModule.hybrid_search`、`LegacyGraphFixtureAdapter`、`ParentDocumentStore` 的旧路径夹具执行轨迹。
- `QueryPlanValidator`、`TargetedGraphRetriever`、`RestrictedVectorRetriever`、`ParentDocumentStore` 的新路径执行轨迹。
- 实体不存在、关系不存在、图服务不可用三类故障注入；关系 fixture 变更反例。
- provenance 绑定、gold 字段全量篡改、个人本地延迟门控不适用声明。

## 结果

- 关系记录临时改为 `ingredient-missing` 后，old/new 均从 driver 返回得到 `graph_status=verified`、`evidence=["graph"]` 和“图查询返回了关系记录。”；没有路由标签伪造 not-found。
- 原始 fixture 下 old/new 报告均 `valid=true`，50 条覆盖率 100%，Recall@5 1.0，证据完整率 1.0，证据 linkage 1.0，关系路径正确率 1.0，禁止断言 0，严格营养误报 0，故障降级违规 0。
- `relation-missing` old 行实际触发 PDS 查询时记录 `ParentDocumentStore`，与 manifest 的逐路由组件契约一致。
- fixture P95 明确为 `not_applicable_fixture_component_budget`，不作为服务或线上性能门控。
- manifest 严格拒绝 fixture、cases、thresholds、results、baseline report 或 provenance 不匹配。

## 验证命令

```bash
PYTHONDONTWRITEBYTECODE=1 python -m pytest -q -p no:cacheprovider test/test_retrieval_fixture_runner.py test/test_retrieval_refactor_eval.py
```

结果：`12 passed`。隔离 `/tmp` 逐字复现四条 manifest 命令通过；当前仓库全量回归为 `197 passed`，仅 6 条既有 PytestCollectionWarning。

## 限制

这是个人本地组件契约夹具，不连接真实 Neo4j、Milvus、staging 或线上流量；不构成真实服务 schema、导入、用户 UI/API 或线上 rollout 证据。`protected` profile 的真实环境门槛仍未签收。
