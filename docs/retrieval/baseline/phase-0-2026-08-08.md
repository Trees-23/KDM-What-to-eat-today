# 阶段 0：环境与回归基线

## 阶段状态

**状态：passed。** A1、A2、B、C 均已完成并留档。本地 Docker 目标由用户在本轮明确授权，仅启动 Neo4j/Milvus 及其依赖；未启动自动导入、backend 或前端，未删除、清空或重建任何卷/集合。

- 记录时间：2026-08-08T17:44:10+08:00
- 操作者：Codex（`/root`）
- 分支：`codex/retrieval-phase-0`
- A1 修复提交：`08fa347bb6d77aa811124da0676f094b32d009a8`（`修复（图审计）：同步子图测试替身签名`）
- A2/B/C 证据采集：本地目标容器 `what-to-eat-neo4j-a2`、`what-to-eat-milvus-standalone`，采集时间 2026-08-08T17:36–17:44+08:00。
- 基线所基于的 Python：`3.13.12`（Anaconda，Linux WSL2）
- pytest：`9.0.3`
- Python 依赖锁定状态：仅发现 `requirements.txt`，未发现 `poetry.lock`、`Pipfile.lock`、`uv.lock` 或 `requirements*.lock`；`requirements.txt` SHA-256 为 `f5e86a44b602bfa09ee157006afefe4f6565a86086a10e1e47d7712817c6e43e`。

本记录不包含 URI、主机、端口、用户名、密码、token、完整连接串、原始数据或个人信息。

## 门槛结果

| 门槛 | 状态 | 最小证据与结论 |
| --- | --- | --- |
| A1：无服务回归 | passed | 指定四个测试文件退出码为 0，汇总为 `14 passed, 1 warning`。保留的 warning 是 pytest 不收集带构造函数的 `TestGraphRetrieval` 测试辅助类，不影响两个实际测试。 |
| A2：Neo4j 运行实例 | passed | 本地只读目标 `what-to-eat-neo4j-a2`，database=`neo4j`；labels、relationshipTypes、indexes 及三项关系计数查询成功。 |
| A2：Milvus 运行实例 | passed | 本地只读目标 `what-to-eat-milvus-standalone`，database=`default`、collection=`cooking_knowledge`；`describe_collection()`、`get_collection_stats()`、load state 和索引查询成功。 |
| B：CSV 导入可用性 | passed | 运行实例 `server.directories.import=/import`；`/import/nodes.csv` 可读，固定 `LOAD CSV ... RETURN row LIMIT 1` 成功返回一行。未执行导入。 |
| C：稳定 ID 与分类 | passed | 五类节点稳定 ID 均非空且唯一；CookingStep 使用 `CONTAINS_STEP.stepOrder`，TechniqueChunk 使用 `HAS_CHUNK.chunkOrder`；Ingredient `category="蔬菜"` 返回 605 个实例，predicate 已验证。 |

## A1：回归与最小修复

初始命令及结果：

```text
python -m pytest -q test/test_hybrid_retrieval_fusion.py test/test_router_audit_a4.py test/test_graph_audit_a6.py test/test_web_audit_a3.py
退出码：1
汇总：13 passed, 1 failed, 1 warning
失败：GraphAuditA6Test.test_subgraph_search_writes_subgraph_and_reasoning_sections
```

失败根因：`GraphRAGRetrieval.extract_knowledge_subgraph(graph_query, query="")` 是当前生产契约，`graph_rag_search()` 传入原始 query；审计测试替身仍只接受 `graph_query`，导致 `takes 2 positional arguments but 3 were given`。这是测试替身过期，不是运行检索行为缺陷。

修复仅修改 `test/test_graph_audit_a6.py`：替身接受可选 `query` 参数，记录该值，并断言子图检索调用透传了 `川菜有什么特色`。未修改 `rag_modules/`、`main.py`、`config.py`、Neo4j 或 Milvus 数据。

针对性验证：

```text
python -m pytest -q test/test_graph_audit_a6.py
退出码：0
汇总：2 passed, 1 warning
```

完整 A1 复验：

```text
python -m pytest -q test/test_hybrid_retrieval_fusion.py test/test_router_audit_a4.py test/test_graph_audit_a6.py test/test_web_audit_a3.py
退出码：0
汇总：14 passed, 1 warning
```

## A2：运行实例与只读目标检查

执行的服务状态命令及结果：

```text
docker compose ps
docker compose -f docker-compose.dev.yml ps
docker compose -f data/docker-compose.yml ps
退出码：均为 0
结果：均无运行服务。
```

客户端可用性检查结果：

```text
宿主默认 Python：neo4j missing，pymilvus missing。
用户授权后创建临时隔离环境 `/tmp/retrieval-phase0-a2-venv`，安装 `neo4j` 与 `pymilvus==2.5.11`，退出码：0。
```

执行命令：

```bash
python - <<'PY'
import importlib.util
import platform
import sys
for package in ('neo4j', 'pymilvus'):
    print(f'{package}:', 'available' if importlib.util.find_spec(package) else 'missing')
print('python:', sys.version.replace('\n', ' '))
print('platform:', platform.platform())
PY
```

运行时、测试工具和依赖锁定检查命令（退出码均为 0）：

```bash
date --iso-8601=seconds
python -m pytest --version
sha256sum requirements.txt
rg --files -g 'poetry.lock' -g 'Pipfile.lock' -g 'uv.lock' -g 'requirements*.lock' -g 'requirements*.txt' -g 'pyproject.toml' -g 'Pipfile' | sort
```

已做的安全只读配置检查：

- 本地 `.env` 未被 Git 跟踪且被 `.gitignore` 忽略；只确认变量名是否存在，未读取或记录任何值。
- 应用配置包含 Neo4j/Milvus 的本地默认值和环境变量入口，但这不是运行实例授权，也不能据此猜测目标。
- 未发现 `.github/`、`docs/`、`deploy/`、`deployment/` 下的受保护部署配置、阶段基线、只读目标标识或审批记录。
- 历史状态（2026-08-08T04:56+08:00）：当时未执行 A2 所要求的查询，因此尚无 labels、relationshipTypes、indexes、database、collection、schema、维度或行数可记录。本轮用户授权本地 Docker 后，该状态已由下列 A2 实例证据取代。

目标与审批记录候选的只读文件发现命令：

```bash
rg --files -g '.github/**' -g 'docs/**' -g 'deploy/**' -g 'deployment/**' -g '*approval*' -g '*baseline*' -g '!node_modules' | sort
```

该命令退出码为 0；执行时未返回候选文件。环境变量检查仅枚举已定义的键名，不检查、输出或记录等号后的值，命令退出码为 0：

```bash
sed -n 's/^\([A-Za-z_][A-Za-z0-9_]*\)=.*/\1/p' .env 2>/dev/null | sort
git ls-files --stage .env .env.example
git check-ignore -v .env 2>/dev/null
```

所需的外部前提是已授权目标的脱敏别名及其只读范围：Neo4j 实例/database 与 Milvus 实例/database/collection，并且有可验证的部署或审批记录。目标确认后才可安装并使用对应客户端执行限定的只读查询。

本轮用户已明确授权本地 Docker 开发目标，因此使用临时环境完成了真实只读查询；该临时环境未写入仓库依赖文件。

### Neo4j 实例证据

- 目标别名：`what-to-eat-neo4j-a2`；database：`neo4j`。
- 执行命令（均退出码 0；密码未记录）：

```bash
docker exec what-to-eat-neo4j-a2 /var/lib/neo4j/bin/cypher-shell --database neo4j -u neo4j -p <redacted> 'CALL db.labels()'
docker exec what-to-eat-neo4j-a2 /var/lib/neo4j/bin/cypher-shell --database neo4j -u neo4j -p <redacted> 'CALL db.relationshipTypes()'
docker exec what-to-eat-neo4j-a2 /var/lib/neo4j/bin/cypher-shell --database neo4j -u neo4j -p <redacted> 'SHOW INDEXES'
docker exec what-to-eat-neo4j-a2 /var/lib/neo4j/bin/cypher-shell --database neo4j -u neo4j -p <redacted> 'MATCH (:Recipe)-[r:REQUIRES]->(:Ingredient) RETURN count(r)'
docker exec what-to-eat-neo4j-a2 /var/lib/neo4j/bin/cypher-shell --database neo4j -u neo4j -p <redacted> 'MATCH (:Recipe)-[r:CONTAINS_STEP]->(:CookingStep) RETURN count(r)'
docker exec what-to-eat-neo4j-a2 /var/lib/neo4j/bin/cypher-shell --database neo4j -u neo4j -p <redacted> 'MATCH (:TechniqueDoc)-[r:HAS_CHUNK]->(:TechniqueChunk) RETURN count(r)'
```

- labels：`Recipe`、`Ingredient`、`CookingStep`、`Root`、`CookingMethod`、`CookingTool`、`DifficultyLevel`、`RecipeCategory`、`Category`、`ConceptType`、`GraphStatistics`、`RelationshipStatistics`、`TechniqueDoc`、`TechniqueChunk`。
- relationshipTypes：`REQUIRES`、`CONTAINS_STEP`、`BELONGS_TO`、`DIFFICULTY_LEVEL`、`BELONGS_TO_CATEGORY`、`HAS_CONCEPT_TYPE`、`SIMILAR`、`NEXT_STEP`、`USES_SAME_TOOL`、`USES_SAME_METHOD`、`HAS_CHUNK`。
- 三项计数：`REQUIRES=2905`、`CONTAINS_STEP=2514`、`HAS_CHUNK=120`。
- `SHOW INDEXES` 返回 21 个 ONLINE 索引：`cookingstep_fulltext_index`、`cookingstep_id_unique`、`cookingstep_number_index`、`index_343aff4e`、`index_f7700477`、`ingredient_category_index`、`ingredient_fulltext_index`、`ingredient_id_unique`、`ingredient_name_index`、`recipe_cuisine_index`、`recipe_difficulty_index`、`recipe_fulltext_index`、`recipe_id_unique`、`recipe_name_index`、`technique_chunk_fulltext_index`、`technique_chunk_id_unique`、`technique_chunk_title_index`、`technique_doc_category_index`、`technique_doc_fulltext_index`、`technique_doc_id_unique`、`technique_doc_name_index`。范围/全文/lookup 类型及字段结果已在命令输出中核验。
- 五类节点稳定 ID 样本与唯一性：Recipe `323/323`、Ingredient `2906/2906`、CookingStep `2514/2514`、TechniqueDoc `18/18`、TechniqueChunk `120/120`（总数/非空且去重 ID 数）。名称/别名字段抽样确认 `name`、`preferredTerm`、`synonyms`。
- CookingStep：`CONTAINS_STEP.stepOrder` 非空 `2514/2514`；关系上的 `step_order` 与 `stepNumber` 均为空，节点 `CookingStep.stepNumber` 作为同值步骤号抽样存在。
- TechniqueChunk：`HAS_CHUNK.chunkOrder` 与节点 `chunkIndex` 均非空 `120/120`。
- Ingredient 分类：`蔬菜=605`、`调料=1547`、`蛋白质=327`、`淀粉类=170`、`其他=251`、`脂肪=4`、`工具=1`，另有 1 个 category 为空；`category="蔬菜"` 的只读 predicate 返回 605 个实例，确认可执行。

### Milvus 实例证据

- 目标别名：`what-to-eat-milvus-standalone`；database：`default`；collection：`cooking_knowledge`。
- 客户端版本：`pymilvus 2.5.11`；集合 `auto_id=false`、动态字段关闭、1 个 partition、1 个 shard。
- 执行命令（退出码 0）：在 `/tmp/retrieval-phase0-a2-venv` 中以 `MilvusClient(uri=<local-alias>, db_name='default')` 执行 `list_collections()`、`describe_collection('cooking_knowledge')`、`get_collection_stats('cooking_knowledge')`、`get_load_state('cooking_knowledge')`、`list_indexes('cooking_knowledge')` 和 `describe_index('cooking_knowledge', '_default_idx_101')`。
- 行数：`1333`；load state：`Loaded`（客户端原始状态 `3`）；索引：`_default_idx_101`，field=`vector`、type=`HNSW`、metric=`COSINE`、`M=16`、`efConstruction=200`、indexed/total/pending rows=`1333/1333/0`、state=`Finished`。
- schema：`id` 主键 VarChar(150)、`vector` FloatVector(dim=512)、`text` VarChar(15000)、`node_id` VarChar(100)、`recipe_name` VarChar(300)、`node_type` VarChar(100)、`category` VarChar(100)、`cuisine_type` VarChar(200)、`difficulty` Float、`doc_type` VarChar(50)、`chunk_id` VarChar(150)、`parent_id` VarChar(100)。
- collection 描述、统计和 load state 均为只读查询；未创建、删除、重建或写入 collection。

## B：CSV 可读性

静态检查确认了初始挂载风险，但运行实例查询已证明本地 Neo4j 的实际 import 目录为 `/import`，因此 B 已通过：

- `data/cypher/neo4j_import.cypher` 通过 `file:///nodes.csv` 和 `file:///relationships.csv` 导入 Recipe 图数据。
- 根 `docker-compose.yml` 与 `docker-compose.dev.yml` 将具名卷挂载到 `/var/lib/neo4j/import`，同时把仓库 `./data/cypher` 挂载到 `/import`。
- `SHOW SETTINGS` 返回 `server.directories.import=/import`；容器 `/import` 可读 `nodes.csv`、`relationships.csv` 和技巧 CSV。

未修改 Compose、挂载、CSV 或导入脚本。用户授权后仅替换了卡在 GDS 下载阶段的本地 Neo4j 容器启动方式，仍使用原有数据卷；未执行 `neo4j-init` 或任何导入。

执行命令与结果（均退出码 0）：

```bash
docker exec what-to-eat-neo4j-a2 /var/lib/neo4j/bin/cypher-shell --database neo4j -u neo4j -p <redacted> "SHOW SETTINGS YIELD name, value WHERE name = 'server.directories.import' RETURN name, value"
# 返回：server.directories.import=/import
docker exec what-to-eat-neo4j-a2 /var/lib/neo4j/bin/cypher-shell --database neo4j -u neo4j -p <redacted> "LOAD CSV WITH HEADERS FROM 'file:///nodes.csv' AS row RETURN row.nodeId AS nodeId LIMIT 1"
# 返回：一行 nodeId=100000000
```

```cypher
LOAD CSV WITH HEADERS FROM 'file:///nodes.csv' AS row
RETURN row
LIMIT 1;
```

### 本地启动与数据保护审计

启动前的只读卷检查结果：`neo4j_data` 为 `521.5MB`、`milvus_data` 为 `351.0MB`、`neo4j_import` 为 `4.0KB`；三个具名卷均创建于 2026-05-01。原有 Neo4j/Milvus 数据已经存在，故没有执行任何“补充导入”或集合构建。

启动命令为：

```bash
docker compose up -d neo4j milvus-standalone
```

该命令只请求 `neo4j`、`milvus-standalone` 及 Milvus 的 `etcd`/`minio` 依赖，未请求 `neo4j-init`、backend、frontend 或 nginx。根 Neo4j 容器因 GDS 插件下载未监听而被停止；随后以相同的既有 Neo4j data/log/import 卷和 `/import` CSV bind mount 创建了 `what-to-eat-neo4j-a2`，仅启用 APoC，未运行 Cypher 导入脚本。

审计命令与状态：

```bash
docker inspect <container> --format '<created/status/started/finished/restart>'
docker inspect <container> --format '<mount type/source/destination/rw>'
```

- `what-to-eat-neo4j-a2` 创建并启动于 2026-08-08，挂载原有 `neo4j_data`、`neo4j_logs`、`neo4j_import` 卷和仓库 `data/cypher -> /import` bind mount。
- `what-to-eat-milvus-standalone` 使用原有 `milvus_data` 卷。
- `what-to-eat-neo4j-init` 最近一次结束时间为 2026-05-09，并未在本轮启动；当前仍为 exited。

此记录证明本轮没有调用显式的 Neo4j 导入、Milvus 建库/删库、集合重建或卷删除命令；服务启动自身可能更新日志或运行时元数据，故不把它表述为“零底层写入”。

## C：稳定 ID、排序与分类

以下实例事实已由本地 Neo4j 只读抽样验证，状态为 passed：

| 节点类型 | 需要由实例确认的 ID/名称映射 | 需要由实例确认的排序或分类 |
| --- | --- | --- |
| Recipe | `nodeId`、名称与别名来源 | 无 |
| Ingredient | `nodeId`、名称与别名来源 | 分类字段或分类关系、枚举、空值比例、可执行蔬菜 predicate |
| CookingStep | `nodeId`、所属 Recipe | `CONTAINS_STEP.stepOrder`，缺失时 `stepNumber` |
| TechniqueDoc | `nodeId`、名称/标题/标签来源 | 无 |
| TechniqueChunk | `nodeId`、所属 TechniqueDoc | `HAS_CHUNK.chunkOrder`，缺失时 `chunkIndex` |

执行的 C 核验查询均退出码 0：对五类 label 分别执行 `count(n)`、`count(n.nodeId)`、`count(DISTINCT n.nodeId)`；对步骤执行 `count(c.stepOrder)` 与节点 `count(s.stepNumber)`；对技巧块执行 `count(c.chunkOrder)` 与 `count(n.chunkIndex)`；对 Ingredient 执行 `category` 分组、`IS NULL`、空字符串和参数化 `WHERE i.category = $category` 查询。结果为五类 ID 均完整且唯一；`stepOrder=2514/2514`、`stepNumber=2514/2514`，技巧块的优先与回退排序字段均为 `120/120` 非空；分类空值 `1/2906`、空字符串 `0/2906`，蔬菜 predicate 返回 `605`。

`data/cypher/generate_tips_csv.py` 仍是 TechniqueDoc/TechniqueChunk Markdown 生产器；未发现 Recipe Markdown 到 `nodes.csv`/`relationships.csv` 的对应可复现生产器。该缺口不阻塞当前从既有 Neo4j 只读物化的阶段 1，但仍是阶段 6 数据生产前提。

## 当前用户场景基线

图数据库只读探针已执行，但 backend 未启动，因此以下不是 staging UI/API 的最终用户验收；它们只记录当前实例可观察事实，不能替代后续阶段的独立场景签收。

| 固定场景 | 当前结果 | 原因 |
| --- | --- | --- |
| 宫保鸡丁怎么做？ | graph probe available | Recipe 精确命中 1 个，关联 CookingStep 9 个；正文回补尚未实现。 |
| 宫保鸡丁第一步怎么腌？ | graph probe available | Recipe 稳定 ID 与 `stepOrder=1` 可定位；正文窗口尚未实现。 |
| 鸡肉能做什么？ | graph probe available | Ingredient 精确命中 1 个，关联 Recipe 1 个。 |
| 鸡肉搭配什么蔬菜？ | graph probe not_found | 已验证 `category="蔬菜"`，当前图路径返回 0 个目标蔬菜；不能用文本补造关系。 |
| 推荐低脂川菜 | unavailable | 营养治理未冻结；阶段 0 不作推荐或低脂结论。 |
| 夏天吃什么清淡的？ | unavailable | backend、PDS 和受限向量路径尚未启用。 |
| 腌肉有哪些关键要点和适用场景？ | graph probe available | TechniqueDoc 内容包含“腌”的文档 4 个；连续章节回补尚未实现。 |
| 实体不存在 | graph probe not_found | `阶段0不存在的实体` 精确查询返回 0 个。 |
| 图关系不存在 | graph probe not_found | 当前关系类型中无目标关系；未让文本充当关系证明。 |
| 图服务失败 | not exercised | 未注入服务故障；留待后续降级验收。 |

## 回退、差异与下一步

- feature flag：本阶段无新增或变更的运行开关。
- 回退：没有数据、配置或服务变更需要回退；测试修复仅同步测试替身与既有生产接口。
- 与 V2/实施方案的差异：V2 第 4 节将 Recipe 生产闭环称为“门槛 C”，而实施方案 V1 与当前执行提示将阶段 0 的 C 定义为稳定 ID 与分类。按执行优先级，本阶段把 C 记录为 ID/分类门槛；Recipe 生产器缺口另列为阶段 6 前提，未擅自改变任一契约。
- 下一步：A1、A2、B、C 已通过，且本地数据无需重复导入；完成本次基线提交/复核后可开始阶段 1。阶段 1 仍不得启动任何 Milvus collection 重建或生产数据迁移。

## PR 状态

`08fa347` 已推送至 `origin/codex/retrieval-phase-0`。本机 `gh` 未配置 GitHub 认证，自动创建草稿 PR 被该外部认证条件阻塞；建议 PR 地址为：

```text
https://github.com/Trees-23/KDM-What-to-eat-today/pull/new/codex/retrieval-phase-0
```
