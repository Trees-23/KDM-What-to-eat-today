# 阶段 0：环境与回归基线

## 阶段状态

**状态：blocked。** A1 已通过；A2、B、C 缺少已授权且可核验的运行实例，不能以本地默认配置、停止的容器、CSV 或导入脚本替代真实服务证据。

- 记录时间：2026-08-08T04:56:01+08:00
- 操作者：Codex（`/root`）
- 分支：`codex/retrieval-phase-0`
- A1 修复提交：`08fa347bb6d77aa811124da0676f094b32d009a8`（`修复（图审计）：同步子图测试替身签名`）
- 基线所基于的 Python：`3.13.12`（Anaconda，Linux WSL2）
- pytest：`9.0.3`
- Python 依赖锁定状态：仅发现 `requirements.txt`，未发现 `poetry.lock`、`Pipfile.lock`、`uv.lock` 或 `requirements*.lock`；`requirements.txt` SHA-256 为 `f5e86a44b602bfa09ee157006afefe4f6565a86086a10e1e47d7712817c6e43e`。

本记录不包含 URI、主机、端口、用户名、密码、token、完整连接串、原始数据或个人信息。

## 门槛结果

| 门槛 | 状态 | 最小证据与结论 |
| --- | --- | --- |
| A1：无服务回归 | passed | 指定四个测试文件退出码为 0，汇总为 `14 passed, 1 warning`。保留的 warning 是 pytest 不收集带构造函数的 `TestGraphRetrieval` 测试辅助类，不影响两个实际测试。 |
| A2：Neo4j 运行实例 | blocked | 三份 Compose 配置均无运行服务；Python 环境缺少 `neo4j` 客户端；没有已授权的只读 Neo4j 实例别名、database 或批准记录。未连接，也未安装客户端。 |
| A2：Milvus 运行实例 | blocked | Python 环境缺少 `pymilvus` 客户端；没有已授权的只读 Milvus 实例别名、database、collection 或批准记录。未连接，也未安装客户端。 |
| B：CSV 导入可用性 | blocked | B 的前置 A2 未通过。静态配置显示 `file:///nodes.csv` 与 Neo4j 实际 import 目录的挂载路径尚未在授权运行实例中验证；未读取容器 import 目录，未执行 `LOAD CSV`。 |
| C：稳定 ID 与分类 | blocked | C 只能基于 A2 的实例样本确认五类节点的 ID、排序字段、别名来源及 Ingredient 的可执行蔬菜 predicate。当前没有实例查询结果，未以 Cypher/CSV/自然语言推断替代。 |

## A1：回归与最小修复

初始命令及结果：

```text
python -m pytest -q test/test_hybrid_retrieval_fusion.py test/test_router_audit_a4.py test/test_graph_audit_a6.py test/test_web_audit_a3.py
退出码：1
汇总：13 passed, 1 failed, 1 warning
失败：GraphAuditA6Test.test_subgraph_search_writes_subgraph_and_reasoning_sections
```

失败根因：`GraphRAGRetrieval.extract_knowledge_subgraph(graph_query, query="")` 是当前生产契约，`graph_rag_search()` 传入原始 query；审计测试替身仍只接受 `graph_query`，导致 `takes 2 positional arguments but 3 were given`。这是测试替身过期，不是运行检索行为缺陷。

修复仅修改 `test/test_graph_audit_a6.py`：替身接受可选 `query` 参数，记录该值，并断言子图检索调用透传了 `川菜有什么特色`。未修改 `rag_modules/`、`main.py`、`config.py`、Neo4j 或 Milvus 数据。

针对性验证：

```text
python -m pytest -q test/test_graph_audit_a6.py
退出码：0
汇总：2 passed, 1 warning
```

完整 A1 复验：

```text
python -m pytest -q test/test_hybrid_retrieval_fusion.py test/test_router_audit_a4.py test/test_graph_audit_a6.py test/test_web_audit_a3.py
退出码：0
汇总：14 passed, 1 warning
```

## A2：运行实例与只读目标检查

执行的服务状态命令及结果：

```text
docker compose ps
docker compose -f docker-compose.dev.yml ps
docker compose -f data/docker-compose.yml ps
退出码：均为 0
结果：均无运行服务。
```

客户端可用性检查结果：

```text
neo4j: missing
pymilvus: missing
退出码：0
```

执行命令：

```bash
python - <<'PY'
import importlib.util
import platform
import sys
for package in ('neo4j', 'pymilvus'):
    print(f'{package}:', 'available' if importlib.util.find_spec(package) else 'missing')
print('python:', sys.version.replace('\n', ' '))
print('platform:', platform.platform())
PY
```

运行时、测试工具和依赖锁定检查命令（退出码均为 0）：

```bash
date --iso-8601=seconds
python -m pytest --version
sha256sum requirements.txt
rg --files -g 'poetry.lock' -g 'Pipfile.lock' -g 'uv.lock' -g 'requirements*.lock' -g 'requirements*.txt' -g 'pyproject.toml' -g 'Pipfile' | sort
```

已做的安全只读配置检查：

- 本地 `.env` 未被 Git 跟踪且被 `.gitignore` 忽略；只确认变量名是否存在，未读取或记录任何值。
- 应用配置包含 Neo4j/Milvus 的本地默认值和环境变量入口，但这不是运行实例授权，也不能据此猜测目标。
- 未发现 `.github/`、`docs/`、`deploy/`、`deployment/` 下的受保护部署配置、阶段基线、只读目标标识或审批记录。
- 未执行 A2 所要求的 `CALL db.labels()`、`CALL db.relationshipTypes()`、`SHOW INDEXES`、三个关系计数、Milvus `describe_collection()` 或 `get_collection_stats()`；因此没有 labels、relationshipTypes、indexes、database、collection、schema、维度或行数可记录。

目标与审批记录候选的只读文件发现命令：

```bash
rg --files -g '.github/**' -g 'docs/**' -g 'deploy/**' -g 'deployment/**' -g '*approval*' -g '*baseline*' -g '!node_modules' | sort
```

该命令退出码为 0；执行时未返回候选文件。环境变量检查仅枚举已定义的键名，不检查、输出或记录等号后的值，命令退出码为 0：

```bash
sed -n 's/^\([A-Za-z_][A-Za-z0-9_]*\)=.*/\1/p' .env 2>/dev/null | sort
git ls-files --stage .env .env.example
git check-ignore -v .env 2>/dev/null
```

所需的外部前提是已授权目标的脱敏别名及其只读范围：Neo4j 实例/database 与 Milvus 实例/database/collection，并且有可验证的部署或审批记录。目标确认后才可安装并使用对应客户端执行限定的只读查询。

## B：CSV 可读性

静态检查只确认下列待验证风险，不能作为 B 通过证据：

- `data/cypher/neo4j_import.cypher` 通过 `file:///nodes.csv` 和 `file:///relationships.csv` 导入 Recipe 图数据。
- 根 `docker-compose.yml` 与 `docker-compose.dev.yml` 将具名卷挂载到 `/var/lib/neo4j/import`，同时把仓库 `./data/cypher` 挂载到 `/import`。
- 已停止的容器不能证明授权目标的实际 import 目录、CSV 可见性或 Neo4j 的文件导入策略。

未修改 Compose、挂载、CSV、导入脚本或容器。B 只有在 A2 目标已确认后，先从同一授权容器/服务器读取实际 import 目录，再在同一 database 成功执行以下只读预检才可通过：

```cypher
LOAD CSV WITH HEADERS FROM 'file:///nodes.csv' AS row
RETURN row
LIMIT 1;
```

## C：稳定 ID、排序与分类

以下实例事实均未验证，状态为 blocked：

| 节点类型 | 需要由实例确认的 ID/名称映射 | 需要由实例确认的排序或分类 |
| --- | --- | --- |
| Recipe | `nodeId`、名称与别名来源 | 无 |
| Ingredient | `nodeId`、名称与别名来源 | 分类字段或分类关系、枚举、空值比例、可执行蔬菜 predicate |
| CookingStep | `nodeId`、所属 Recipe | `CONTAINS_STEP.stepOrder`，缺失时 `stepNumber` |
| TechniqueDoc | `nodeId`、名称/标题/标签来源 | 无 |
| TechniqueChunk | `nodeId`、所属 TechniqueDoc | `HAS_CHUNK.chunkOrder`，缺失时 `chunkIndex` |

静态发现只说明后续风险，不替代实例证据：`data/cypher/generate_tips_csv.py` 是 TechniqueDoc/TechniqueChunk Markdown 生产器；未发现 Recipe Markdown 到 `nodes.csv`/`relationships.csv` 的对应可复现生产器。此生产器缺口是阶段 6 的数据生产前提，不能用于推断当前图实例的稳定 ID 或分类。

## 当前用户场景基线

由于 A2 未通过，以下场景均**未在真实 Neo4j/Milvus 服务或 staging UI/API 中执行**。不能将现有单元测试、停止的 Compose 容器或静态 CSV 作为用户场景验收，故当前实际结果记为 unavailable，而不是通过或失败。

| 固定场景 | 当前结果 | 原因 |
| --- | --- | --- |
| 宫保鸡丁怎么做？ | unavailable | 无已授权运行实例。 |
| 宫保鸡丁第一步怎么腌？ | unavailable | 无已授权运行实例。 |
| 鸡肉能做什么？ | unavailable | 无已授权运行实例。 |
| 鸡肉搭配什么蔬菜？ | unavailable | 无已授权运行实例，且蔬菜 predicate 未核验。 |
| 推荐低脂川菜 | unavailable | 无已授权运行实例；营养治理仍未冻结。 |
| 夏天吃什么清淡的？ | unavailable | 无已授权运行实例。 |
| 腌肉有哪些关键要点和适用场景？ | unavailable | 无已授权运行实例。 |
| 实体不存在 | unavailable | 无已授权运行实例。 |
| 图关系不存在 | unavailable | 无已授权运行实例。 |
| 图服务失败 | unavailable | 无已授权运行实例。 |

## 回退、差异与下一步

- feature flag：本阶段无新增或变更的运行开关。
- 回退：没有数据、配置或服务变更需要回退；测试修复仅同步测试替身与既有生产接口。
- 与 V2/实施方案的差异：V2 第 4 节将 Recipe 生产闭环称为“门槛 C”，而实施方案 V1 与当前执行提示将阶段 0 的 C 定义为稳定 ID 与分类。按执行优先级，本阶段把 C 记录为 ID/分类门槛；Recipe 生产器缺口另列为阶段 6 前提，未擅自改变任一契约。
- 下一步持续检查：仅在可用的受保护部署配置、审批记录或已运行服务中安全检查是否出现授权只读目标；在 A2、B、C 全部通过并完成独立审查前，不创建阶段 1 代码、PDS、Milvus collection 或迁移脚本。

## PR 状态

`08fa347` 已推送至 `origin/codex/retrieval-phase-0`。本机 `gh` 未配置 GitHub 认证，自动创建草稿 PR 被该外部认证条件阻塞；建议 PR 地址为：

```text
https://github.com/Trees-23/KDM-What-to-eat-today/pull/new/codex/retrieval-phase-0
```
