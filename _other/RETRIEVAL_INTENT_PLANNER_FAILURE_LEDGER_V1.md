# 意图规划验收失败台账

## 回归规则

全量正式题库仅用于建立基线和第 9.5 节最终验收。一次全量运行完成后，运行器输出的
`failure-summary.json` 是唯一允许的后续回归输入：下一轮从当前官方题库按其中的
`question_id` 重新冻结题目。单题失败不会中断同一轮，也不会触发 300 题重跑。

每轮结束后：

1. 保留逐题 JSONL、审计目录、验收报告和失败摘要。
2. 从通过的题目中移除；仅对遗留失败题建立下一轮冻结回归集。
3. 先集中完成同类根因诊断与最小修复，再执行下一轮失败集回归。
4. 只有失败集清零且第 9.5 节其他闸门齐备时，才重新执行 300 题最终验收。

不得手工改写题干、替换题号、伪造图事实或用题号/固定 ID 绕过编译器。

## 当前冻结失败集

来源运行：`run/intent-planner-acceptance/2026-08-14-planner-failure-regression-007/`

- 官方题库 SHA-256：`e5edfbced75e84002bdd5b25054bf0906a022b4073e03ab31a4c85010f494f79`
- 运行代码提交：`021ca039ae75cd2e1da5de52108e73c19e05c668`
- 覆盖：2/2；通过：1；遗留失败：1；审计记录：完整；空成功：0。
- 下一轮只允许以该目录的 `failure-summary.json` 作为 `--failure-summary` 输入。

| 题号 | 当前状态 | 可审计根因 | 所需治理动作 |
| --- | --- | --- | --- |
| `S07-C-04` | `NO_PREFERENCE_RESULTS` | `番茄`已治理合并到规范实体`西红柿`（`201003210`），旧节点`201002557`已移除，全部 12 条 `REQUIRES` 边已改指向规范节点。2026-08-14 的隔离 Neo4j 真实导入核验确认：规范食材关联 12 道菜，但`cuisineType='川菜'`的交集仍为 0；`西红柿炒鸡蛋`存在但其菜系为空。 | 由数据/题库负责人补齐并审核至少一条“川菜且使用西红柿”的来源事实，或修订该题的受治理 gold 标签。现有实现必须保持硬范围为空即终止，不能扩大为全库检索。 |

## 已收敛项

`S07-B-01`、`S07-B-05`、`S07-C-08` 已在同一轮通过。其根因是 Milvus 2.3 对过长
`parent_id in [...]` 过滤的检索缺陷；修复后按最多 20 个父级分批受限检索并在本地聚合，
审计包含 `filter_batch_count`，未扩大检索范围。

## 解除条件

`S02-C-07` 已解除：隔离 Neo4j 从清理后 CSV 重建、PDS build
`pds_3e852335769f5178ffb574c5` 健康检查通过，并在失败集回归中以 verified 图事实和 1 条 PDS
正文证据通过。当前运行中的默认图/PDS/Milvus 未被改写。

剩余项解除前必须同时具备：治理变更的来源与审核记录、PDS/图/Milvus manifest 一致性核验、
对应题目的审计证据、失败集回归通过。剩余失败集清零后，才可安排下一次全量 300 题运行。
