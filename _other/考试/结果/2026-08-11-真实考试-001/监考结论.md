# 监考结论

## 结论

**不可签收。** 本轮未进入 API 考试阶段，也未将任何 HTTP 200、历史审计、fixture 或组件单测描述为本次考试通过。

## 版本与完整性

- 运行编号：`2026-08-11-真实考试-001`
- 实现版本：`68bf4c3ea5f73352ee1900978d7f20dfab4f074e`
- 题库 SHA-256：`d4bb3d8495e41b4daeed280e673cb884c59cb917b470f147bfa9a2ca8423208b`
- old：300/300 条，全部 `blocked`。
- new：300/300 条，全部 `blocked`。
- `gold_manifest.json` 未关闭（`gold_manifest_closed=false`），没有伪造 gold、排名、SSE/HTTP 响应、审计目录或组件断言。

## 根因

S05 要求每题冻结至少一条真实 `Ingredient <- REQUIRES - Recipe - REQUIRES -> 蔬菜` 图路径。稳定节点和关系 CSV 与 Neo4j 的只读 nodeId 查询一致表明下列题没有此类路径：

- `S05-A-03` 鸡肉
- `S05-B-09` 玉米
- `S05-C-02` 西兰花
- `S05-C-08` 南瓜
- `S05-C-09` 菠菜

其中 `S05-A-03` 的鸡肉 nodeId `201004679` 存在一条菜谱关系，但该菜谱没有满足 `category=蔬菜` 的第二条 `REQUIRES` 边。由于这与题库的最少路径契约冲突，继续请求 API 将无法在请求前关闭全题 gold，故按规则停止。

## 指标与安全

old/new 均没有可计分的 completed 排名题，因此 Recall、Precision、Hit Rate、MRR、nDCG、路线/证据/忠实度和延迟指标均无有效分母。禁止断言、无依据关系断言和严格营养误报计数均为 0，但这是未运行的阻断结果，不能解释为安全通过。

## 重开条件

先修复或重新基线化 S05 的缺失图路径与题库契约，并在只读预检中验证所有 30 个 S05 实体均有至少一条路径；随后使用新的运行编号重新冻结完整 gold，再执行 old/new 服务考试及 S09/S10 隔离组件考试。
