# 监考结论

## 结论

**不可进入全量考试**

本次只运行 `new`，共记录 10 题，`new.jsonl` 通过 schema 校验，题号无重复，且未运行 `old`。正式开考预检退出码为 0、`status=ready`；三类断言计数均为 0，S05-C-04 与 S08-A-02 的安全拒答均通过。

但存在 1 条 `error` 与 2 条 `blocked`，不满足定向放行的“10 题完整覆盖、没有 blocked/error、所有路线和证据满足门槛”要求。

## 阻断项

- `S05-A-02`：`error`。目标图查询 `ingredient_vegetable_pairs_v1`、图事实与 PDS 回补均已生成，但原始 SSE 仅含 `data: [DONE]`，审计记录 `chunk_count=0`、`answer_chars=0`，无法复核最终关系回答。原始审计：[rag_process.md](audits/new/S05-A-02/20260812_061725_654_af9dbc9c/rag_process.md)。
- `S06-A-02`：`blocked`。实际路由为 `entity_not_found`，未进入要求的 `restricted_vector`，也没有排序候选或 PDS 回补。原始审计：[rag_process.md](audits/new/S06-A-02/20260812_062254_062_b99fa6f5/rag_process.md)。
- `S07-A-02`：`blocked`。实际路由为 `entity_not_found`，未进入要求的 `restricted_vector`，也没有排序候选或 PDS 回补。回答没有严格营养误报，但路由与证据门槛未满足。原始审计：[rag_process.md](audits/new/S07-A-02/20260812_062329_410_8c59c2a2/rag_process.md)。

## 已满足项

- S01-A-02、S01-B-01 完成实体直达与 PDS 正文回补，审计未出现线程或 parent store 故障，且 `vector_search_calls=0`。
- S02-A-02 使用 `recipe_step_anchor_v1`，S03-A-02 使用 `technique_chunks_v1`；两题均有 PDS 正文回补且未走全文向量检索。
- S04-A-02 使用 `ingredient_recipes_v1`，图事实和候选正文可复核。
- S05-C-04 实际为 `graph_not_found`，回答明确“无法证明”，没有关系断言。
- S08-A-02 实际为 `entity_not_found`，明确未收录，未生成做法、食材或步骤。

完整逐题路径、候选、PDS 回补、最终回答与计数见 [路径与召回明细.md](路径与召回明细.md)；汇总覆盖和指标见 [测试结果总评.md](测试结果总评.md)。
