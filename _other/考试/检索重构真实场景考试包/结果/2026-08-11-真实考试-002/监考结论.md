# 监考结论

## 结论

**不可签收。** 本次运行在真实服务考试开始前被预检阻断。没有把 HTTP 200、历史审计、离线 fixture、静态 CSV 路径或组件单测称为本次 API 考试通过。

## 版本与完整性

- 运行编号：`2026-08-11-真实考试-002`
- 实现提交：`6b13a41f7dab1b99e1e737d29ba50873e60416f0`
- 题库 SHA-256：`688edfea11db27a5cf5796ffa886e785f73c1f2ceb6a714f879830b3d06df88d`
- gold：`gold_manifest.json` 已于请求前关闭，覆盖 300 题。
- old：300/300 行，全部 `blocked`，无重复、无 schema 问题。
- new：300/300 行，全部 `blocked`，无重复、无 schema 问题。
- 未请求 API，因此 `responses/` 和 `audits/` 仅有未执行说明；S09/S10 未运行，`components/` 未伪造脚本输出或断言。

## Old/New 对比

两变体均未启动，亦没有实际检索路线、候选排序、PDS 回补、生成回答或延迟样本。因此 old/new 在总体和所有场景 S01-S10 的 Recall、Precision、Hit Rate、MRR、nDCG、路线准确率、证据完整性/链接性、忠实度、安全率、P50/P95 延迟均无有效分母，不能比较提升或退化。

唯一可确认的差异结论是：`new` 所需的 Milvus V2 artifact 文件存在，但其运行时服务不可用，故不能将该文件当成新路径可用或成功证据。

## 阻断根因

1. `milvus-standalone`、`milvus-etcd`、`milvus-minio` 都以 exit `255` 退出；backend 无法连接 `milvus-standalone:19530` 并持续重启，`/health` 返回状态 `000`。
2. Neo4j 的题库稳定节点 ID 均可解析，但 S05 A/B 的运行时 `category="蔬菜"` 多跳查询均为零，与 CSV 中已冻结的正向路径契约不一致。S05 C 的零路径反例则按预期为零。

实际观测路线只有 `preflight_blocked_runtime_dependency`，并非 `old` 或 `new` 的检索路线；没有受控回退可计分。

## 安全计数

每个 blocked 行的禁止断言、无依据关系断言、严格营养误报计数均为 0，但这些是“没有生成回答”的记录，**不等同于安全测试通过**。没有遗漏题或重复题，却存在 600 条 blocked 行、无 API 审计和无可复核候选，因此结论只能为不可签收。

## 重开条件

恢复并健康验证 Milvus 依赖及 backend `/health`，再只读复核 S05 A/B 的运行时图路径语义与题库静态契约一致。之后以新的运行编号重新冻结 gold，依序启动 old/new 变体，逐题串行执行 S01-S08 并保留审计，再独立运行 S09/S10。
