# 监考结论

## 结论

**不可签收。** 本次真实服务考试在任何 API 请求前被预检阻断，不能将服务健康、静态工件存在、HTTP 200、历史考试记录或离线 fixture 表述为本次 old/new 检索路径通过。

## 版本与完整性

- 运行编号：`2026-08-11-真实考试-003`
- 实现提交：`bdecb3e69c62cb478a5dcbcdd393c1551db75730`
- 题库 SHA-256：`688edfea11db27a5cf5796ffa886e785f73c1f2ceb6a714f879830b3d06df88d`
- gold：`gold_manifest.json` 未关闭，明确记录为 `preflight_status=blocked`，没有编造不符合题库名称契约的 S03 gold。
- old：300/300 行，全部 `blocked`，无重复或格式问题。
- new：300/300 行，全部 `blocked`，无重复或格式问题。
- 未执行 `/api/chat/stream`，因此 `responses/`、`audits/` 和 `components/` 均只保留未执行说明；S09/S10 没有伪造隔离组件断言。

## 阻断根因

S03 的 20/30 题无法按题库要求同时以 `TechniqueDoc` 名称和 `sourcePath` 唯一解析。相关 Markdown 和当前 `tips_nodes.csv` 中的 TechniqueDoc 均存在，但节点标题与题库 `entity_name` 不一致。例如 `S03-A-01` 的题库名称为“如何选择现在吃什么”，同一 sourcePath 的当前节点名称为“如何决策吃什么”。完整逐题证据在 `preflight.md` 及 `components/预检只读查询结果.json`。

该问题使 300 题的请求前 gold 无法完整冻结。按考试规则，不能以 sourcePath 单独匹配、以文本相似性替换命名实体，或在请求后根据答案补写 gold，因此本次不启动 old/new Compose 覆盖，也不发送请求。

## 已完成的只读观察

- backend `/health` 为 healthy，Milvus V2 artifact 与 PDS 文件可读取。
- S04 全部 30 题的实际直接图路径非零；S05 A/B 全部 20 题的实际多跳图路径非零。
- S05 C 全部 10 题的实际多跳路径为零，符合其反例契约，不是阻断原因。
- S08 的 30 个虚构菜名和 S09 的 30 个虚构食材在节点表中均为零命中。

这些仅说明阻断范围，不构成 S01-S10 的 API 或组件级通过结果。

## Old/New 对比与安全计数

两变体均未运行，因而不存在实际检索路线、候选排序、PDS 回补、生成回答或延迟样本。Recall、Precision、Hit Rate、MRR、nDCG、路线准确率、证据完整性/链接性、忠实度、安全率和 P50/P95 延迟均没有有效分母，不能比较提升或退化。

每个 blocked 行的禁止断言、无依据关系断言和严格营养误报计数均为 0，仅表示没有生成回答，**不等于安全测试通过**。实际观测路线为 `preflight_blocked_gold_resolution`，不是 `old` 或 `new` 路径，也不存在可计分的受控回退。

## 重开条件

将题库 S03 的 `entity_name` 与当前 TechniqueDoc 稳定节点标题/受治理别名和 sourcePath 对齐，重新以新的运行编号完成 gold 预冻结。之后才可顺序运行 S01-S08 的两组真实 SSE 请求、复制对应审计，并独立执行 S09/S10 的假驱动组件考试。

等待用户确认后合并 main。
